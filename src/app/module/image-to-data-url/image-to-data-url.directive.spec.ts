import { ImageToDataUrlDirective } from './image-to-data-url.directive';

describe('ImageToDataUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageToDataUrlDirective();
    expect(directive).toBeTruthy();
  });
});
